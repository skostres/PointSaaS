@using System.Web.Optimization;
<!DOCTYPE html>
<html lang="en">
<head>
    <base href="/" />
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>PCMC</title>

    @Styles.Render("~/Content/bootstrap")
    @Styles.Render("~/Content/font-awesome")
    @Styles.Render("~/Content/sb-admin")
    @Styles.Render("~/Content/site")


    @Scripts.Render("~/bundles/jquery",
                    "~/bundles/bootstrap",
                    "~/bundles/plugins/flot",
                    "~/bundles/plugins/morris")
</head>
<body ng-app="mainApp" ng-controller="AuthCtrl as auth">

    <div login-dialog></div>

    <div ng-if="!visible">
        <div id="wrapper">

            <!-- Navigation -->
            <div ng-include="'Client/Navigation/Navigation.html'" ng-controller="NavigationCtlr as nav"></div>

            <!-- Current Page -->
            <div ui-view>

            </div>
        </div>
        <!-- /#wrapper -->

        <!--
        <div class="footer">
            POWERED BY:
            <img src="~/Content/logos/angular.png" height="60" />
        </div>
            -->
        
                
        <!--
        <div ng-hide="true" style="position:absolute; right:10px; top:61px; z-index:9000" ng-controller="NotificationsCtrl as noti">
            <div ng-repeat="n in noti.notificationList">
                <div draggable id="{{n.UUID}}" ng-if="!n.expired" class="alert alert-dismissable {{noti.getClassOfNotification(n.type)}} draggable" ng-class="{'fade-out': n.expired}">
                    <button type="button" showNotifications class="close" ng-click="noti.setExpiredNotification(n)">×</button>
                    <i class="fa fa-info-circle"></i> {{n.message}}
                </div>
            </div>
        </div>-->

    </div>
    <!-- jumbotron -->
    <!--ALERT NOTIFICATIONS-->
    <div style="position:absolute; right:10px; top:61px; z-index:9000">
        <div growl limit-messages="3"></div>
    </div>

    @RenderBody()
    @Scripts.Render("~/bundles/angular")
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.1.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <!-- <script src="~/Client/Hubs/"></script>-->
</body>
</html>